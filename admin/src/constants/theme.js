const { createTheme } = require("@mui/material");

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#1cacdc",
    },
    secondary: {
      main: "#f50000",
    },
  },
});
